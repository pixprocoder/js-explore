const friends = [22, 5, 24, 66, 11, 73, 223, 55, 668, 32, 26, 221];

const partial = friends.slice(3, 6);
console.log(partial);

/**
 * in Array slice can copy something from an array it start from the given number (1,4) this example it will take from 1 to 3. (slice cannot change the original elements form an array)
 */
