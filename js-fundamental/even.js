/**
 * 1. define a variable
 * 2. give a condition
 * 3. change the variable state
 */

// Target: find the even numbers
var evenNum = 0;
while (evenNum <= 20) {
  //   console.log(evenNum);
  evenNum = evenNum + 2;
}

// Target: Find the odd numbers
var oddNum = 1;
while (oddNum <= 10) {
  console.log(oddNum);
  oddNum = oddNum + 2;
}
