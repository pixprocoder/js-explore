/**
 * Array
 * [2, 4 'etc']
 * get element[]
 * set element[] = ;
 * index (Always start with 0)
 * find position variable.indexOf(name)
 * add to last element: variable.push(name)
 * remove from last: variable.pop(name)
 * length of an array: variable.length
 */

/**
 * Conditional
 * <, >, <=, >=, ==, ===, !=, !==
 * &&, ||
 * if (condition){}
 * else if (condition){}
 * else{}
 */
